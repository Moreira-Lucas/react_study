const MyChildComponent = ({ name, age }) => {
    return (
      <div>
        <h1>Meu nome é {name}</h1>
        <h2>Minha idade é {age}</h2>
      </div>
    );
  };
  
  export default MyChildComponent;